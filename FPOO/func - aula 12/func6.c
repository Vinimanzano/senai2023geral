//função que retorna a média de três parâmetros inteiros
int media(int a, int b, int c){
	return (a + b + c) / 3;
}

int main() {
	printf("A media de 84 + 16 + 100 / 3 = %d", media(84, 16, 100));
	return 0;
}
