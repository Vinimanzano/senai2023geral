//função que retorna a média de dois parâmetros inteiros
int media(int a, int b, int c){
	return (a + b + c) / 2;
}

int main() {
	printf("A media de 84 + 16 + 100 / 2 = %d", media(84, 16, 100));
	return 0;
}
